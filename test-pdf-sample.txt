# Test PDF Documentation

This is a sample document that mentions UserService and AuthController.

## API Endpoints

GET /api/users
POST /api/auth/login

## Code Examples

```typescript
function getUserById(id: string) {
  return UserService.findOne(id);
}
```

The AuthController handles authentication using JWT tokens.
