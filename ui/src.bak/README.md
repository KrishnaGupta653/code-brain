# React + Sigma UI (Superseded)

This React + Sigma.js UI implementation has been superseded by the vanilla Canvas implementation in `ui/public/`.

The vanilla Canvas version has more completed features:
- Level of Detail (LOD) system for large graphs
- WebSocket live updates
- Full-text search
- Better performance on large graphs

This directory is kept for reference only and is not built or deployed.
